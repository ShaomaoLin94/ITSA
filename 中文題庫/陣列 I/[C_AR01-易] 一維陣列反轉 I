#include <bits/stdc++.h>
using namespace std;

int main(void)
{
	vector<int> arr;
    int num;
    while(scanf("%d ", &num)!=EOF){
        arr.push_back(num);
    }
	int size = arr.size();
    for(int i=size-1; i>=0; i--){
        cout << arr[i];
        if(i>0){
            cout << ' ';
        }
    }
    cout << '\n';
	return 0;
}
