#include <bits/stdc++.h>
using namespace std;

int main(void)
{
    vector<long long> arr(6);
    for (int i = 0; i < 6; i++) {
        cin >> arr[i];
    }

    for (int i = 5; i >= 0; i--) {
        cout << arr[i];
        if (i > 0) {
            cout << ' ';
        }
    }
    cout << '\n';
    return 0;
}
