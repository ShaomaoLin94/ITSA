#include <bits/stdc++.h>
using namespace std;

int main(int argc, char *argv[]) {

    string str;
    while (cin >> str) {
        for (int i = str.size() - 1; i >= 0; i--) {
            cout << str[i];
        }
        cout << endl;
    }

    return 0;
}
