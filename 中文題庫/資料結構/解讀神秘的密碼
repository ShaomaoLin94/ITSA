#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(int argc, char *argv[]) {

    char str[1000];
    while(scanf("%s", str)!=EOF){
        for(int i=strlen(str)-1; i>=0; i--){
            printf("%c", str[i]);
        }
        printf("\n");
    }



	return 0;
}
