#include <bits/stdc++.h>
using namespace std;

int main(int argc, char *argv[]) {

    char str;
    int cnt = 0;
    while (cin.get(str)) {
        if (str == '2') {
            cnt++;
        }
    }

    cout << cnt << endl;

    return 0;
}
