#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(int argc, char *argv[]) {

    char str;
    int cnt = 0;
    while(scanf("%c", &str)!=EOF){
        if(str == '2'){
            cnt++;
        }
    }

    printf("%d\n", cnt);

	return 0;
}
