#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[]) {

    long long num1, num2;
    
    while(scanf("%lld %lld", &num1, &num2)!=EOF){
        long long sum = num1+num2;
        printf("%lld\n", sum*sum);
    }

	return 0;
}
