#include <bits/stdc++.h>
using namespace std;

int main(int argc, char *argv[]) {

    long long num1, num2;
    while (cin >> num1 >> num2) {
        long long sum = num1 + num2;
        cout << sum * sum << endl;
    }

    return 0;
}
