#include <bits/stdc++.h>
using namespace std;

int main(int argc, char *argv[]) {

    int mile;
    while (cin >> mile) {
        double km = round((mile * 1.6) * 10) / 10;
        cout << fixed << setprecision(1) << km << endl;
    }

    return 0;
}
