#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[]) {

    int mile;
    while(scanf("%d", &mile)!=EOF){
        double km = round((mile*1.6)*10)/10;
        printf("%.1f\n", km);
    }


	return 0;
}
