#include <bits/stdc++.h>
using namespace std;

int main(int argc, char *argv[]) {

    double c;
    while (cin >> c) {
        double f = round((1.8 * c + 32) * 10) / 10;
        cout << fixed << setprecision(1) << f << endl;
    }

    return 0;
}

// c = (f-32)*(5/9)
