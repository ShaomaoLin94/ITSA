#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[]) {

    double a, b, c;
    while(scanf("%lf %lf %lf", &a, &b, &c)!=EOF){
        
        double ans = (a+b)*c/2;
        printf("Trapezoid area:%.1f\n", ans);
        
    }
    
	return 0;
}
