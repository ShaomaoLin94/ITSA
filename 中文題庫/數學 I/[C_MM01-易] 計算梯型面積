#include <bits/stdc++.h>
using namespace std;

int main(int argc, char *argv[]) {

    double a, b, c;
    while (cin >> a >> b >> c) {
        double ans = (a + b) * c / 2;
        cout << fixed << setprecision(1) << "Trapezoid area:" << ans << endl;
    }

    return 0;
}
