#include <bits/stdc++.h>
using namespace std;

int main(int argc, char *argv[]) {

    int distance;
    double speed = 0.238;
    while (cin >> distance) {
        int time = (int)ceil(distance / speed);
        cout << time << endl;
    }

    return 0;
}
