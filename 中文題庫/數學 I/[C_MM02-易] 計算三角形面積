#include <bits/stdc++.h>
using namespace std;

int main() {

    int base, height;
    cin >> base >> height;
    cout << fixed << setprecision(1) << (double)(base * height) / 2 << endl;

    return 0;
}
